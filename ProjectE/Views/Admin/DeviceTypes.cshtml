@model IEnumerable<ProjectE.Models.DeviceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "DeviceTypeCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "DeviceTypeEdit", new { id=item.DeviceTypeId }) |
            @Html.ActionLink("Delete", "DeviceTypeDelete", new { id=item.DeviceTypeId })
        </td>
    </tr>
}

</table>
